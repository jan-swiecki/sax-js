{
  "name": "sax",
  "description": "An evented streaming XML parser in JavaScript",
  "author": "Isaac Z. Schlueter <i@izs.me> (http://blog.izs.me/)",
  "contributors": [
    "Jan Święcki <jan.swiecki@gmail.com>"
  ],
  "version": "1.3.0-prealpha",
  "main": "dist/sax.js",
  "type": "commonjs",
  "types": "dist/sax.d.ts",
  "license": "ISC",
  "scripts": {
    "build": "bin/build",
    "test": "bin/build --for-testing && tap dist/test --cov -j4 --branches=60 --functions=60 --lines=60 --statements=60",
    "posttest": "standard test/*.js",
    "git-publish": "npm test && npm run build && git add . && git commit -m 'new distribution' && git push origin dev",
    "preversion": "npm test",
    "postversion": "npm publish",
    "postpublish": "git push origin --all; git push origin --tags"
  },
  "repository": "git://github.com/isaacs/sax-js.git",
  "files": [
    "/dist"
  ],
  "devDependencies": {
    "@types/lodash": "^4.14.172",
    "@types/node": "^16.6.1",
    "@types/tap": "^15.0.5",
    "standard": "^16.0.3",
    "tap": "^15.0.9",
    "through2": "^4.0.2"
  },
  "dependencies": {
    "lodash": "^4.17.21"
  }
}
